services:
  mongodb:
    image: mongo:7.0
    container_name: jobfair-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: